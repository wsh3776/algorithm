// head top
数组模拟栈
int stk[N], tt;

// 插入
stk[++ tt] = x;

// 删除
tt --;

// 判断栈是否为空
if (t > 0) not empty
else empty

// 取出栈顶元素
stk[tt];

数组模拟队列
// 在队尾插入元素，在队头弹出元素
int q[N], hh, tt = -1;

// 插入
q[++ tt] = x;

// 弹出
hh ++;

// 判断队列是否为空
if (hh <= tt) not empty
else empty

// 取出队头元素
q[hh]

// 取出队尾元素
q[tt]

